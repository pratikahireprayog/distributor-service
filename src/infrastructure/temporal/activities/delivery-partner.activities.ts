export async function executeTestRule(partnerId: string): Promise<string> {
    // This is just a sample activity. Replace with your actual business logic
    console.log(`Executing test rule for partner ${partnerId}`);
    return `Processed partner ${partnerId}`;
} 